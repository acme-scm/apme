# Minimal collection layout — establishes FQCN namespace.
# The GraphBuilder uses this to determine collection ownership of
# modules, plugins, and roles.
---
name: Graph_patterns
namespace: testorg
version: 1.0.0
readme: README.md
description: Test collection for ContentGraph edge type coverage
